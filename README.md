# graphql

##To start graphql server
```
cd graphql-sequelize-server
yarn
docker-compose up
```

##To start GraphiQL
```
cd graphiql-ui
yarn
yarn start
```

docker stop $(docker ps -a -q) 
docker rm $(docker ps -a -q)

